import LoginButton from "./components/LoginButton";

const Login = () => {
    return (
        <div>
            <h2>Click on the bellow button to login</h2>
            <LoginButton />
        </div>
    );
};

export default Login;
