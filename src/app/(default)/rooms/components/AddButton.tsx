"use client"

const AddButton = () => {
    const handleAdd = async () => {

    };

    return (
        <div>
            <button onClick={handleAdd}>Add</button>
        </div>
    )
};

export default AddButton;
